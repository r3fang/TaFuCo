import sys
import collections 

d = collections.defaultdict(list)
for line in sys.stdin:
    [edge, read] = line.strip().split()
    d[edge].append(read)

for name in d:
    d_set = set(d[name])
    print '>'+name+'\t'+str(len(d_set))
    for read in d_set:
        print read
